# ── Secretos y configuración local: JAMÁS se versionan ──────────────────────
.env.local
# Tokens YWH/Intigriti, API key del LLM, master key de cifrado, rutas de recon
.settings/

# ── Build y dependencias ─────────────────────────────────────────────────────
.next/
node_modules/
# Índice SQLite (reconstruible: pnpm run db:reset)
reporter.db
*.db
*.db-shm
*.db-wal

# ── Runtime ──────────────────────────────────────────────────────────────────
.reporter.log
.reporter.pid
*.swp
.DS_Store
